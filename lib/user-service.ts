// Re-exports from the canonical location to support both import paths:
// - @/lib/user-service  (legacy / editor ghost reference)
// - @/services/user.service (canonical location)
export { UserService } from "@/services/user.service";
